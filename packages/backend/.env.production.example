# 生产环境配置示例
NODE_ENV=production
PORT=3001

# 数据库配置
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=roianalyze_user
DB_PASSWORD=your_secure_password
DB_DATABASE=roianalyze_prod

# CORS配置 (生产环境中前后端同域名可设置为同源)
CORS_ORIGIN=http://yourdomain.com

# 其他配置
LOG_LEVEL=info
